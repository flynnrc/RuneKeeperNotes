{
  "name": "RuneKeeperNotes - .NET 8 + Claude Code",

  "image": "mcr.microsoft.com/devcontainers/dotnet:1-8.0",

  "forwardPorts": [5000, 5001],

  "portsAttributes": {
    "5001": {
      "protocol": "https"
    }
  },

  "mounts": [
    "source=runekeeper-claude-config,target=/home/vscode/.claude,type=volume"
  ],

  // Fix volume ownership before installing Claude Code
  "postCreateCommand": "sudo chown -R vscode:vscode /home/vscode/.claude && curl -fsSL https://claude.ai/install.sh | bash && dotnet restore && echo 'alias yolo=\"claude --dangerously-skip-permissions\"' >> ~/.bashrc",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csharp",
        "ms-dotnettools.csdevkit",
        "humao.rest-client",
        "ms-dotnettools.vscodeintellicode-csharp"
      ]
    }
  },

  "remoteUser": "vscode"
}